import React from 'react';
import styled from 'styled-components';

//회원가입, 로그인, 마이페이지 레이아웃 담당하는 컴포넌트

const AuthTemplateBlock = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
`;

export default AuthTemplateBlock;
